# Redundant seit pyproject.toml — verweist nur noch darauf, damit
# "pip install -r requirements.txt" weiter funktioniert (Core + Server-Extra).
# Dev-/Build-Extras separat: pip install -e .[dev]  bzw.  pip install -e .[build]
-e .[server]
